import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "SKRYM - Stream Your Favorite Movies and TV Shows",
  description: "Watch the latest movies and TV shows in high quality. Stream on-demand content with personalized recommendations.",
  keywords: "streaming, movies, TV shows, on-demand, entertainment",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={`${inter.className} bg-black text-white antialiased`}>
        {children}
      </body>
    </html>
  );
}
